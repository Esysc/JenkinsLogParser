services:
  jenkins:
    build: .
    container_name: jenkins-test
    platform: linux/amd64
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - ./jenkins-config/init.groovy.d:/var/jenkins_home/init.groovy.d:ro
      - ./jenkins-config/jobs:/tmp/jenkins-jobs:ro

volumes:
  jenkins_home:
